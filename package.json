{
  "name": "interactive-quiz-app",
  "version": "1.0.0",
  "description": "Production-ready Interactive Quiz App — Vanilla JS, MVC, Observer, Web Workers",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "jsdom": "^24.1.0",
    "@vitest/ui": "^2.0.0"
  }
}
